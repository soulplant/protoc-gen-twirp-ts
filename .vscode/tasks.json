{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "test",
      "type": "shell",
      "command": "go install; and protoc --twirp-ts_out=out --twirp-ts_opt=foo=bar --twirp-ts_opt=bar=baz --go_out=out test.proto"
    }
  ]
}